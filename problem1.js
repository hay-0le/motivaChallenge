// input: 3 arguments start date-time(string), end date-time(string), period-in-days(integer)
//output: list of tuples where Y - X should never equal more than period-in-days
//constraints: try to use higher order function
//edge case: period in days larger than gap between start and end date
        //invalid formats

let createIntervals = (startDateTime, endDateTime, periodInDays = 3) => {

    return [];
}

module.exports = {
    createIntervals: createIntervals
};